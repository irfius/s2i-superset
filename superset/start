#!/bin/bash -e

case $cmd in 
    "usage")
        echo "Usage!"
        /usr/libexec/s2i/usage
        exit 0
        ;;
    "superset-init")
        echo "Running superset initialzations scripts ->>> $fab_username $fab_password"
        source /superset/.venv/bin/activate
        superset db upgrade
        fabmanager create-admin --app $FLASK_APP \
            --username $fab_username \
            --firstname $fab_firstname --lastname $fab_lastname --email $fab_email \
            --password $fab_password
        superset init
        gunicorn \
            -w 4 \
            -k gevent \
            --timeout 120 \
            -b  0.0.0.0:6666 \
            --limit-request-line 0 \
            --limit-request-field_size 0 \
            superset:app
        ;;
    "superset")
        echo "Running superset with Gunicorn"
        superset db upgrade
        gunicorn \
            -w 4 \
            -k gevent \
            --timeout 120 \
            -b  0.0.0.0:6666 \
            --limit-request-line 0 \
            --limit-request-field_size 0 \
            superset:app
        ;;
    *)
        echo "Running bash"
        source /superset/.venv/bin/activate
        exec /bin/bash
        ;;
esac